import { expect, test, describe } from "vitest";
import { IssueBuilder } from "./../src/index";

describe("IssueBuilder", () => {
  test.todo("build() returns valid JSON");
  test.todo("addIssues() works");
  test.todo("addIssues() throws error if autoTaskName already exists");
  test.todo("merge() works");
});
